Files:
server.c:
	- creates the room; 
	- runs all the game mechanics
	- processes messages from client
	- sends messages to client
	- players, cards, deck lives here 
	- (i.e. almost everything)
client.c:
	- solicits user input
	- sends stuff to server
	- displays info from server
algo.c:
	- runs algorithms for sorting, shuffle, hand value
game_mechanics.c:
	- calling
	- betting
	- raising
	- etc.
networking.c:
	- handles sockets
	- handles handshake
	- sends to/from server/client
	- semaphore

algo.c
--------------------------------------------------
*card sort(*card hand)
*card shuffle(*card deck)
int compare_card(*card card1, *card card2)
int hand_value(*card, river, *card hand)

game_mechanics.c
--------------------------------------------------
*player round_winner( list of pointers to player structs figure out the type )
* figure out the rest


